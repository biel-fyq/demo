/* app.wxss */

/* 字体：使用网络地址或移除此段，避免本地字体 500 错误。若需本地字体，请放到项目根目录 /fonts 并使用相对路径。 */
/* @font-face {
  font-family: 'ZCOOLKuaiLe';
  src: url('https://fonts.gstatic.com/s/zcoo/regular.ttf') format('truetype');
  font-weight: normal;
  font-style: normal;
} */
/* 顶部安全区工具类：在 iPhone 刘海、胶囊等设备上留白，其他机型不受影响 */
.safe-top {
	padding-top: constant(safe-area-inset-top);
	padding-top: env(safe-area-inset-top);
}

/* 底部安全区工具类 */
.safe-bottom {
	padding-bottom: constant(safe-area-inset-bottom);
	padding-bottom: env(safe-area-inset-bottom);
}

/* 顶部留白占位：固定高度 + 顶部安全区，避免内容被顶到最上方 */
.safe-top-spacer {
	height: calc(constant(safe-area-inset-top) + 160rpx);
	height: calc(env(safe-area-inset-top) + 160rpx);
	width: 100%;
}
